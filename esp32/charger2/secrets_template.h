/*
 * secrets_template.h — WiFi credential template for charger2
 *
 * HOW TO USE:
 *   1. Copy this file to secrets.h in the same directory.
 *   2. Fill in your actual SSID and password.
 *   3. secrets.h is listed in .gitignore so your credentials won't
 *      accidentally be committed to the repository.
 */
#pragma once

const char* WIFI_SSID     = "YOUR_SSID";
const char* WIFI_PASSWORD = "YOUR_PASSWORD";
